import React from 'react';
import styled from "styled-components";

export const SideBarLeft = () => {
    return (
        <StyledSideBarLeft>

        </StyledSideBarLeft>
    );
};

const StyledSideBarLeft = styled.aside`
    grid-area: sideL;
    background-color: #d4aff8;
`